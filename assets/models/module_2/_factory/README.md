# Module 2 procedural factory

- `module_2_part_factory.gd` — builds network teaching meshes by `part_id`
- `part_model.gd` — scene entry used by every `../<part>/model.tscn`

One part = one sibling folder under `assets/models/module_2/`.
Do not put Sketchfab downloads in `_factory/`.
