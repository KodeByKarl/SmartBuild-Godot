# Module 1 procedural factory

- `module_1_part_factory.gd` — builds clean teaching meshes by `part_id`
- `part_model.gd` — scene entry script used by every `../<part>/model.tscn`

Do not put Sketchfab downloads here. Keep one part per sibling folder.
